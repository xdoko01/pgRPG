{
	"$id": "wait.schema.json",
	"$schema": "https://json-schema.org/draft-07/schema",

	"title": "wait command",
	"description": "Wait for specific amount of time.",
	"markdownDescription": "*Wait* for specific amount of time.",

	"type": "array",
	"prefixItems": [
		{
			"title": "wait command",
			"description": "Wait for specific amount of time.",
			"markdownDescription": "*Wait* for specific amount of time.",		
			"enum": ["wait"]
		},
		{
			"title": "wait command parameters",
			"description": "duration_ms (required)",
			"markdownDescription": "`duration_ms` (required)",
			"type": "object",
			"required": ["duration_ms"],

			"properties": {
				"duration_ms": {
					"title": "Time to wait in ms",
					"description": "Time to wait in miliseconds",
					"markdownDescription": "Time to wait in miliseconds",
					"examples": [100,200,500,1000,2000],
					"type": "integer",
					"minimum": 0
				}
			}
		}
	],
	"items": false
}