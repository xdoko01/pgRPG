{
	"$id": "arm_ammo.schema.json",
	"$schema": "https://json-schema.org/draft-07/schema",

	"title": "arm_ammo command",
	"description": "Arm the entity with a specific ammo pack entity.",
	"markdownDescription": "Arm the entity with a specific *ammo pack* entity.",

	"type": "array",
	"prefixItems": [
		{
			"title": "arm_ammo command",
			"description": "Arm the entity with a specific ammo pack entity.",
			"markdownDescription": "Arm the entity with a specific *ammo pack* entity.",
			"enum": ["arm_ammo"]
		},
		{
			"title": "arm_ammo command parameters",
			"description": "ammo_entity_id (required)",
			"markdownDescription": "`ammo_entity_id` (required)",
			"type": "object",
			"required": ["ammo_entity_id"],

			"properties": {
				"ammo_entity_id": {
					"title": "Ammo Entity",
					"description": "ID or alias of the ammo pack entity to arm.",
					"markdownDescription": "ID or alias of the *ammo pack* entity to arm.",
					"examples": ["wooden_arrows_pack", 42],
					"$ref": "../definitions.schema.json#/definitions/entity_id"
				}
			}
		}
	],
	"items": false
}
