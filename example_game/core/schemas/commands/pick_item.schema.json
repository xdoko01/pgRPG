{
	"$id": "pick_item.schema.json",
	"$schema": "https://json-schema.org/draft-07/schema",

	"title": "pick_item command",
	"description": "Pick up an item and add it to the entity's inventory.",
	"markdownDescription": "Pick up an item and add it to the entity's *inventory*.",

	"type": "array",
	"prefixItems": [
		{
			"title": "pick_item command",
			"description": "Pick up an item and add it to the entity's inventory.",
			"markdownDescription": "Pick up an item and add it to the entity's *inventory*.",
			"enum": ["pick_item"]
		},
		{
			"title": "pick_item command parameters",
			"description": "item_entity_id (required), item_category (required)",
			"markdownDescription": "`item_entity_id` (required), `item_category` (required)",
			"type": "object",
			"required": ["item_entity_id", "item_category"],

			"properties": {
				"item_entity_id": {
					"title": "Item Entity",
					"description": "ID or alias of the item entity to pick up.",
					"examples": ["key", "coin_01", 7],
					"$ref": "../definitions.schema.json#/definitions/entity_id"
				},
				"item_category": {
					"title": "Item Category",
					"description": "Category under which this item is stored in the inventory.",
					"markdownDescription": "Category under which this item is stored in the inventory.",
					"examples": ["keys", "coins", "weapons"],
					"type": "string"
				}
			}
		}
	],
	"items": false
}
