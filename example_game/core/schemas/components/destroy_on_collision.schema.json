{
	"$id": "destroy_on_collision.schema.json",
	"$schema": "https://json-schema.org/draft-07/schema",

	"title": "Destroy On Collision Component",
	"description": "Schema validating the Destroy On Collision component.",

	"examples" : [
		{"type" : "new.destroy_on_collision:DestroyOnCollision", "params" : {"ttl" : 1000}}
	],

	"required": [ "type", "params"],
	"properties": {
		"type": {
			"description" : "Path to the component class.",
			"enum": [ "new.destroy_on_collision:DestroyOnCollision" ]
		},
		"params": {
			"title": "Component parameters",
			"description" : "Parameters of the DestroyOnCollision component class - 'ttl'.",
			"type": "object",
			"$ref": "#/$defs/cond_params",
			"properties": {
				"ttl" : {
					"$ref": "../definitions.schema.json#/definitions/entity_ttl"
				}
			}
		}
	},

	"$defs" : {
		"cond_params": {
			"description" : "Conditions for 'param' properties.",
			"anyOf": [true]
		}
	}
	
}