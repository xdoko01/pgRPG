{
	"$id": "has_score.schema.json",
	"$schema": "https://json-schema.org/draft-07/schema",

	"title": "HasScore Component",
	"description": "Schema validating the HasScore component.",

	"examples" : [
		{"type" : "new.has_score:HasScore", "params" : {"score" : 10}},
		{"type" : "new.has_score:HasScore", "params" : {}}
	],

	"required": [ "type", "params"],
	"properties": {
		"type": {
			"description" : "Path to the component class.",
			"enum": [ "new.has_score:HasScore" ]
		},
		"params": {
			"title": "Component parameters",
			"description" : "Parameters of the HasScore component class - 'score'.",
			"type": "object",
			"$ref": "#/$defs/cond_params",
			"properties": {
				"score" : {
					"$ref": "../definitions.schema.json#/definitions/entity_score"
				}
			}
		}
	},

	"$defs" : {
		"cond_params": {
			"description" : "Conditions for 'param' properties.",
			"anyOf": [true]
		}
	}
	
}