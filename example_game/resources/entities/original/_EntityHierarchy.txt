+ ... means entity is added
++ ... means that inherited component is overwritten
###############################################

model.json		# no component
		|
		-> character.json		# +Collidable 
				|
				-> dark_male.json	# +RenderableModel - animated character
				-> dark_female.json  # +RenderableModel - animated character
				-> light_female.json # +RenderableModel - animated character
				-> orc_female.json 	# +RenderableModel - animated character
				-> orc_male.json 	# +RenderableModel - animated character
				-> tanned_male.json	# +RenderableModel - animated character
		|
		-> weapon.json		# +Collidable	
				|
				-> wooden_bow.json	# +RenderableModel, ++Collidable
		|		-> short_sword.json	# +RenderableModel, ++Collidable

		-> ammo_pack.json 	# +Collidable
				|
				-> wooden_arrows_pack.json		# +RenderableModel, ++Collidable
				-> iron_arrows_pack.json			# +RenderableModel, ++Collidable
				-> fireball_spell_pack.json		# +RenderableModel, ++Collidable

		|
		-> ammo.json	+Collidable
				|
				-> wooden_arrow.json		# +RenderableModel, ++Collidable
				-> iron_arrow.json		# +RenderableModel, ++Collidable
				-> fireball_spell.json	# +RenderableModel, ++Collidable

		|
		-> wearable.json		+Collidable
				|
				-> striped_pants.json		# +RenderableModel, ++Collidable
				-> golden_helmet.json		# +RenderableModel, ++Collidable

thing.json 		# no components
		|
		-> being.json		# +Motion, +Damageable - template for characters
			|
			-> human.json	# +Teleportable, +Brain, +CanTalk - template for human characters
				|
				-> player.json	# +HasInventory, +CanWear, +HasWeapon
		|
		-> weapon.json 		# no components
				|
				-> spell.json	# +Weapon
				-> bow.json		# +Weapon, +Pickable
				-> sword.json	# +Weapon, +Pickable
				-> spear.json	# +Weapon, +Pickable, +Collidable
		|
		-> ammo_pack.json	# no components
				|
				-> wooden_arrows_pack.json	# +Factory, +Pickable, +Collidable
				-> iron_arrows_pack.json	# +Factory, +Pickable, +Collidable
				-> fireball_spell_pack.json	# +Factory, +Pickable, +Collidable
				-> sword_swings_pack.json	# +Factory
		|
		-> ammo.json	# no components
				|
				-> wooden_arrow.json	# +Temporary, +LinearMotion, +Motion, +Damaging
				-> iron_arrow.json	# +Temporary, +LinearMotion, +Motion, +Damaging
				-> fireball_spell.json	# +Temporary, +LinearMotion, +Motion, +Damaging
				-> sword_swing.json		# +Temporary, +Collidable, +Damaging
		|
		-> wearable.json	# no components
				|
				-> pants.json		# +Wearable, +Pickable
				-> helmet.json		# +Wearable, +Pickable

controls.json	# +Controllable by default key profile
		|
		-> controls_1.json	# ++Controllable (by key_controls_1 key_profile)
		-> controls_2.json	# ++Controllable (by key_controls_2 key_profile)
		-> controls_3.json	# ++Controllable (by key_controls_3 key_profile)
		-> controls_4.json	# ++Controllable (by key_controls_4 key_profile)

