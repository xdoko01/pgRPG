+ ... means entity is added
++ ... means that inherited component is overwritten
###############################################

model.json		# no components
		|
		-> character_model.json		# +Collidable 
				|
				-> darkmale.json	# +RenderableModel - animated character
				-> darkfemale.json  # +RenderableModel - animated character
				-> lightfemale.json # +RenderableModel - animated character
				-> orcfemale.json 	# +RenderableModel - animated character
				-> orcmale.json 	# +RenderableModel - animated character
				-> tannedmale.json	# +RenderableModel - animated character
		|
		-> weapon_model.json		# +Collidable	
				|
				-> wooden_bow_model.json	# +RenderableModel, ++Collidable
		|		-> short_sword_model.json	# +RenderableModel, ++Collidable

		-> ammo_pack_model.json 	# +Collidable
				|
				-> wooden_arrows_pack_model.json		# +RenderableModel, ++Collidable
		|
		-> projectile_model.json	+Collidable
				|
				-> wooden_arrow_model.json		# +RenderableModel, ++Collidable
		|
		-> wearable_model.json		+Collidable
				|
				-> striped_pants_model.json		# +RenderableModel, ++Collidable
				-> golden_helmet_model.json		# +RenderableModel, ++Collidable

thing.json 		# no components
		|
		-> being.json		# +Motion, +Damageable - template for characters
			|
			-> human.json	# +Teleportable, +Brain, +CanTalk - template for human characters
				|
				-> player.json	# +HasInventory, +CanWear, +HasWeapon
		|
		-> weapon.json 		# no components
				|
				-> spell.json	# +Weapon
				-> bow.json		# +Weapon, +Pickable
				-> sword.json	# +Weapon, +Pickable
				-> spear.json	# +Weapon, +Pickable, +Collidable
		|
		-> ammo_pack.json	# no components
				|
				-> wooden_arrows_pack.json	# +Factory, +Pickable, +Collidable
				-> sword_swings_pack.json	# +Factory
		|
		-> projectile.json	# no components
				|
				-> wooden_arrow.json	# +Temporary, +LinearMotion, +Motion, +Damaging
				-> sword_swing.json		# +Temporary, +Collidable, +Damaging
		|
		-> wearable.json	# no components
				|
				-> pants.json		# +Wearable, +Pickable
				-> helmet.json		# +Wearable, +Pickable



